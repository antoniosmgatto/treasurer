node_modules/
dist/
.next/
coverage/
*.tsbuildinfo

# Secrets stay out; the example is the one env file that is committed.
# Keep this negation last among the .env rules — a later broad `.env*` would undo it.
.env
.env.*
!.env.example

.vercel/

# Real member data never enters the repository (D8).
*.private.json
data/

# Local PGlite database for development.
.data/

# Next.js build output and generated agent guidance.
apps/web/.next/
apps/web/.playwright-mcp/

# Design working files — sources and exports, kept locally.
docs/design/
